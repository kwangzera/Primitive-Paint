import java.awt.*;
import javax.swing.*;
import java.awt.event.*;

public class Pen extends Draw {
    public Pen(JPanel panel, JButton penButton, int x_pos, int y_pos) {
        super(panel, penButton, x_pos, y_pos);
    }
}
